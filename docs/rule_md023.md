diff

enabled by default, current disabled
- gfm can start anywhere, not just at beginning

atx
- slight diff in the column #... start of line vs start of token

setext
- reports where it occurs, current is just setext
- if setext `---` indented, doesn't catch
- only triggers on first line of multiline header
- fixed inconsistentcy between block quote and list item